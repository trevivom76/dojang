# 주민등록번호는 생년월일과 성별 정보를 포함합니다.
# 801223-1234567는 80년 12월 23일 생일을 의미합니다.
# 이때 두 번째 숫자 리스트가1 또는  2로 시작하면 1900년대에 출생 사람을 의미하고, 1로 시작하면 남성, 2로 시작하면 여성을 의미합니다.
# 2000년대 출생한 남성은 3으로 시작하고 여성은 4로 시작합니다.
#
# 주민등록번호가 입력되면 다음과 같이 주민등록에 포함된 정보를 출력하도록 코드를 작성하세요.
# 주민등록번호 입력 시 중간에 하이픈(-)을 포함해 입력합니다.
#
#
# 결과
# 주민등록번호 입력:801223-1234567
# 남자 1980 년 출생
#
# 주민등록번호 입력:110503-3456789
# 남자 2011 년 출생

# 핵심 잡기
# 변수를 초기화하고 조건문에 따라 변숫값을 유지하거나 변경하면서 코드를 작성할 수 있습니다.
# 초보자는 물론 코드 작성 경험이 많은 프로그래머도 실수를 많이 하는 것이 바로 변수를 초기화하지 않고 사용하는 것입니다.
# 오류가 발생하면 변수의 초깃값 설정이 잘 되었는지 확인해 보세요.
# 1900년대 또는 2000년대에서 연대 초깃값으로 선정하세요.
# 그리고 남자 또는 여자를 하나의 성별로 초깃값으로 설정한 뒤 조건문에 따라 초깃값 변수를 변경하면서 코드를 작성하세요.
# ‘110503-4567890’ 와 같이 주민등록번호를 입력하면 맨 앞에 6개의 숫자는 태어난 연도와 태어난 달과 일을 표현합니다.
# 하이픈(–) 바로 뒤에 숫자가 4이므로 2000년 이후 출생한 여성을 의미합니다.
# 숫자와 문자 사이에 크기 비교 코드를 작성하면서 아래와 같은 에러가 종종 발생합니다.
# TypeError: '<=' not supported between instances of 'str' and 'int'
# 이 오류는 str 문자열과 int 정수 사이에는 <= 비교 연산자를 사용할 수 없다는 의미입니다.
# 물론 문자와 숫자의 크기를 비교할 수 없습니다.
# 이러한 자료형 관련 에러(TypeError)가 나오면 우선 자료형 변환 (type casting)에 문제가 없는지 살펴봐야 합니다.
# int(age)와 같이 문자형 숫자 변수 age를 숫자로 형 변환을 잘했는지,
# str(age)와 같이 숫자형 변수 age를 문자형 숫자로 잘 변환하고 사용하는지 확인이 필요합니다.
#
# # 주민등록번호 입력 받기
# id_number = input("주민등록번호 입력: ")
#
#
# # 주민등록번호에서 생년월일과 성별 정보를 추출
# birth_date = id_number[:6]  # 생년월일 (앞 6자리)
# gender_code = id_number[7]  # 성별을 나타내는 코드 (7번째 문자)
#
# # 생년월일에서 연도 추출
# year = int(birth_date[:2])  # 연도는 생년월일의 첫 2자리
#
# # 성별 코드에 따른 출생연도와 성별 결정
# if gender_code in ['1', '2']:
#     year += 1900
# elif gender_code in ['3', '4']:
#     year += 2000
#
# # 성별 출력
# if gender_code in ['1', '3']:
#     gender = "남자"
# elif gender_code in ['2', '4']:
#     gender = "여자"
# else:
#     gender = "알 수 없음"
#
# # 결과 출력
# print(f"{gender} {year}년 출생")
#

N = list(input("주민등록번호 입력:").split('-'))

year = int(N[0][:2])
g = int(N[1][0])

if g in [1, 2]:
    years = 1900 + year
elif g in [3, 4]:
    years = 2000 + year

if g in [1, 3]:
    g = '남자'
elif g in [2, 4]:
    g = '여자'

print(f'{g} {years} 년 출생')
